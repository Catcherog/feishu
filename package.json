{
  "name": "sop-feishu-v2-migration",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "SOP feishu-v2 迁移工具集（Pilot Writer / Reader / Cleanup）— CommonJS 模块岛",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:pilot": "node --test tests/pilot-vertical-slice.test.js tests/pilot-writer-v1.test.js",
    "stage-b": "node scripts/stage-b-run.js",
    "stage-b:check": "node scripts/stage-b-run.js --check",
    "stage-b:dry-run": "node scripts/stage-b-run.js --dry-run"
  },
  "_workspace_notice": "feishu-v2 必须作为 SOP workspace 子模块运行（不可独立 clone 后直接运行）。pilot-writer-v1.js 通过动态 import('../../../../src/contracts/candidate-v1.js') 加载 SOP ESM 合同（CJS→ESM 桥接）。独立 clone 缺少 SOP/src/contracts/ 目录将导致 Candidate V1 校验失败。"
}
