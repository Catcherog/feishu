# Schema v1.1 字段写入路径报告（R5）

> **生成时间**：2026-07-18 (Asia/Shanghai)
> **执行者**：Trae (GLM-5.2)
> **任务规格**：`docs/ai/tasks/TASK-003-R5-V11-FIELD-VALIDATION-PACKET.md`
> **工作仓库**：`feishu-v2/`（Catcherog/feishu）
> **基线 HEAD**：`82365a0436aff554e8f7bd5318518caeab993208`（R3+R4 P0/P1 修复批次 backfill commit）
> **R5 主批次最终 HEAD**：`3df9fc5da09c751f28629d053951a50374138dda`（含 SHA backfill）
> **R5 修复批次基线 HEAD**：`3df9fc5da09c751f28629d053951a50374138dda`（MVP_FAIL 后修复批次起点）
> **R5 修复批次最终 HEAD**：将在 R5 修复批次 backfill commit 后填入
> **目标 Base 别名**：`V2_PILOT_BASE_ALIAS`（10 张表，真实 token 见 `config/resource-map.local.json`，gitignored）

---

## 1. 摘要

R5 在 V2 测试 Base 上完成 Schema v1.1 delta 的最小应用与字段写读验证。所有 35 个新增字段在 Base 中存在且类型正确；合法值写入后可按相同语义读回；8 项非法值写入全部被飞书 API 拒绝；35 项默认值检查覆盖三种实际行为。6 条合成验证记录按 rollback plan 全部删除并验证 `record_not_found`。Schema delta 保留以备 R6，仅文档化回滚计划。

| 指标 | 结果 |
|---|---|
| Schema diff（机器生成） | 35 added_fields / 1 enum_changes / 4 view_changes / 2 state_machine_changes |
| Patch plan（确定性） | 35 ADD_FIELD（29 create + 6 NO_OP_ALREADY_MATCHED）+ 1 EXTEND_ENUM + 4 ADD_VIEW |
| 实际应用 | 40 operations / 0 failures |
| 字段数变化 | customer 23→37 / project 23→37 / resource 23→30 |
| 字段读回验证 | 35/35 PASS |
| 非法值拒绝 | 8/8 REJECTED_AS_EXPECTED |
| 默认值检查 | 35（27 NO_DEFAULT + 3 marker + 5 NO_DEFAULT_LIVE_BUT_SCHEMA_SPECIFIES_DEFAULT） |
| 合成记录清理 | 6/6 DELETED + 6/6 verified NOT_FOUND_AS_EXPECTED |
| 安全扫描（staged） | S0=0 S1=0 S2=0 |

---

## 2. Schema v1.0 → v1.1 Delta

来源：`schemas/schema-diff-v1.0-to-v1.1.json`（机器生成的确定性 diff）

### 2.1 新增字段（35）

| 实体 | 数量 | 字段键示例 |
|---|---:|---|
| customer | 14 | `budget_parse_rule_version`, `budget_parse_status`, `budget_range_raw`, `canonical_business_id`, `duplicate_review_status`, `legacy_status_raw`, `migrated_at`, `migration_batch_id`, `migration_rule_version`, `migration_source_record_id`, `migration_source_table`, `source_channel_mapping_version`, `source_channel_raw`, `status_mapping_rule_version` |
| project | 14 | `currency`, `deal_amount`, `deposit_amount`, `feedback_collected_at`, `legacy_status_raw`, `migrated_at`, `migration_batch_id`, `migration_rule_version`, `migration_source_record_id`, `migration_source_table`, `payment_status`, `satisfaction_note`, `satisfaction_score`, `status_mapping_rule_version` |
| resource | 7 | `canonical_business_id`, `duplicate_review_status`, `migrated_at`, `migration_batch_id`, `migration_rule_version`, `migration_source_record_id`, `migration_source_table` |

### 2.2 枚举扩展（1）

| 字段 | 表 | v1.0 spec | v1.1 spec | v1.1 新增选项 | v1.1 移除选项 |
|---|---|---|---|---|---|
| 来源渠道（`source_channel`） | customer | 6 | 12 | 抖音, 视频号, 朋友圈, 微信公众号, 微信私聊, 线下活动, 未知 | 微信 |

注 1：v1.0 spec 6 项 = 小红书、微信、官网、小程序、转介绍、其他。v1.1 spec 12 项 = 小红书、抖音、视频号、朋友圈、微信公众号、微信私聊、官网、小程序、转介绍、线下活动、其他、未知。`微信` 在 v1.1 中由 `微信私聊` 取代。

注 2：R5 Task 3 EXTEND_ENUM 仅添加 7 个 v1.1 新增选项，未移除 v1.0 的 `微信`。执行后 live Base 有 14 options（6 v1.0 + 1 pre-existing test option `不存在的渠道XYZ` + 7 v1.1 新增），与 v1.1 spec 12 项不一致（多出 `微信` 和 `不存在的渠道XYZ`）。GPT 复审 R5 时判定为 P0-1 阻塞，违反 TASK-003 Stop Condition。

注 3：R5 修复批次按用户决策 `cleanup_converge_to_v1.1` 收敛枚举：将引用 `微信` 的 1 条测试记录重映射为 `微信私聊`，然后删除 `微信` 和 `不存在的渠道XYZ` 两个额外选项。修复后 live Base 有 12 options，与 v1.1 spec 完全一致。详见第 5.3 节。

### 2.3 视图新增（4）

来源：`schemas/schema-diff-v1.0-to-v1.1.json` 的 `view_changes` 数组。4 个视图分别针对 customer/project/resource 表，filter/sort 配置记录在 patch plan 中。

### 2.4 状态机规则更新（2，仅文档）

`state_machine_changes` 仅规则数组新增一条迁移相关规则，状态和转移不变，无需 Base 结构操作。

---

## 3. Patch Plan 与执行

### 3.1 写前基线验证

V2 测试 Base 在应用 delta 前已确认为 v1.0 基线：

| 表 | v1.0 字段数 | 实际字段数 | matched | missing | extra | is_v10_baseline |
|---|---:|---:|---:|---:|---:|---|
| customer | 23 | 23 | 23 | 0 | 0 | ✓ |
| project | 23 | 23 | 23 | 0 | 0 | ✓ |
| resource | 23 | 23 | 23 | 0 | 0 | ✓ |

### 3.2 执行结果

| 操作类型 | created/extended | no_op | failed | 总计 |
|---|---:|---:|---:|---:|
| ADD_FIELD | 29 | 6 | 0 | 35 |
| EXTEND_ENUM | 1 | 0 | 0 | 1 |
| ADD_VIEW | 4 | 0 | 0 | 4 |
| **总计** | **34** | **6** | **0** | **40** |

6 个 NO_OP_ALREADY_MATCHED 字段是 V2 测试 Base 在 R5 之前已存在的字段（类型一致），无需重建。

### 3.3 写后字段数

| 表 | 写前 | 写后 | delta | 预期 delta |
|---|---:|---:|---:|---|
| customer | 23 | 37 | +14 | ✓ |
| project | 23 | 37 | +14 | ✓ |
| resource | 23 | 30 | +7 | ✓ |

### 3.4 关键限制

`lark-cli +field-create` 不支持 `property`、`formatter`、`currency_code`、`precision`、`default_value` 等样式/默认值键。所有字段仅以最小类型定义创建（type + options）。Schema 中指定的默认值（见第 5 节）需由迁移脚本在写入记录时显式设置。

---

## 4. 字段写读验证

验证标记：`R5_VALIDATION_20260718`（不使用 `MIGRATION_PILOT_001`）

### 4.1 合法值写读

每张表创建 1 条合成记录，写入所有 v1.1 新增字段的合法值，读回并比较。

| 表 | 验证字段数 | PASS | FAIL |
|---|---:|---:|---:|
| customer | 14 | 14 | 0 |
| project | 14 | 14 | 0 |
| resource | 7 | 7 | 0 |
| **总计** | **35** | **35** | **0** |

### 4.2 非法值拒绝

对带 `illegal_value` 的字段尝试写入非法值，预期被飞书 API 拒绝。

| 表 | 字段 | 类型 | 非法值 | 结果 | API 错误码 |
|---|---|---|---|---|---|
| customer | 预算解析状态 | single_select | `INVALID_ENUM_OPTION` | REJECTED_AS_EXPECTED | 800030005 (not_found) |
| customer | 重复候选审核状态 | single_select | `INVALID_STATUS` | REJECTED_AS_EXPECTED | 800004135 (method limited) |
| project | 币种 | single_select | `USD_NOT_ALLOWED` | REJECTED_AS_EXPECTED | 800030005 (not_found) |
| project | 成交金额 | number | `not_a_number` | REJECTED_AS_EXPECTED | 800010407 (cell value shape) |
| project | 定金金额 | number | `also_not_a_number` | REJECTED_AS_EXPECTED | 800010407 (cell value shape) |
| project | 付款状态 | single_select | `INVALID_PAYMENT` | REJECTED_AS_EXPECTED | 800030005 (not_found) |
| project | 客户满意度评分 | number | `not_a_score` | REJECTED_AS_EXPECTED | 800010407 (cell value shape) |
| resource | 重复候选审核状态 | single_select | `INVALID_STATUS` | REJECTED_AS_EXPECTED | 800030005 (not_found) |

**总计：8/8 REJECTED_AS_EXPECTED**。所有非法值写入被拒绝，未产生脏数据。

### 4.3 默认值行为

每张表创建 1 条仅含 marker 字段的记录，读回检查 v1.1 新增字段的默认值。

| 状态 | 数量 | 含义 |
|---|---:|---|
| NO_DEFAULT | 27 | 字段无 schema 默认值，Base 中也为 null（符合预期） |
| HAS_VALUE_NO_SCHEMA_DEFAULT | 3 | "迁移批次ID" 字段，因创建时设置了 marker 值 |
| NO_DEFAULT_LIVE_BUT_SCHEMA_SPECIFIES_DEFAULT | 5 | Schema 指定默认值但 Base 未应用（见下表） |

**5 个 schema 默认值未在 Base 中应用**：

| 表 | 字段 | schema 默认值 | Base 实际值 | 原因 |
|---|---|---|---|---|
| customer | 预算解析规则版本 | `budget-map-v1.0` | null | lark-cli `+field-create` 不支持 `default_value` |
| customer | 来源渠道映射版本 | `source-map-v1.0` | null | 同上 |
| customer | 状态映射规则版本 | `status-map-v1.0` | null | 同上 |
| project | 币种 | `CNY` | null | 同上 |
| project | 状态映射规则版本 | `status-map-v1.0` | null | 同上 |

**处理决策**：迁移脚本在写入记录时必须显式设置这 5 个字段的默认值，不依赖 Base 层默认值机制。

---

## 5. 回滚演练

### 5.1 合成记录清理（已执行）

| 表 | 目标记录数 | 已删除 | 失败 | 验证 |
|---|---:|---:|---:|---|
| customer | 2 | 2 | 0 | NOT_FOUND_AS_EXPECTED |
| project | 2 | 2 | 0 | NOT_FOUND_AS_EXPECTED |
| resource | 2 | 2 | 0 | NOT_FOUND_AS_EXPECTED |
| **总计** | **6** | **6** | **0** | **ALL NOT_FOUND_AS_EXPECTED** |

### 5.2 Schema delta 回滚计划（仅文档，未执行）

Schema delta 保留以备 R6 迁移使用。如需回滚，按以下顺序执行（**DOCUMENTED ONLY, NOT EXECUTED**）：

1. 删除 4 个新增视图（`+view-delete`）
2. 清理 source_channel 枚举：确认无记录引用 7 个新增选项后移除（飞书不允许移除正在使用的选项）
3. 删除 29 个新增字段（`+field-delete`）
4. 6 个 NO_OP_ALREADY_MATCHED 字段无需处理（R5 之前已存在）

### 5.3 R5 修复批次：source_channel 枚举收敛（已执行）

R5 主批次提交后，GPT 复审判定 `MVP_FAIL`，主要阻塞 P0-1 为 live `source_channel` 14 options 与 v1.1 spec 12 项不一致。修复包：`docs/ai/tasks/TASK-003-R5-REVIEW-FIX-PACKET.md`。

#### 5.3.1 只读统计（P0-1 Step 1）

只读脚本 `src/scripts/temp/r5_enum_usage_count.js`（gitignored）扫描 V2 测试 Base Customer 表全量记录，统计两个额外选项的引用数：

| 额外选项 | 引用记录数 |
|---|---:|
| `微信` | 1 |
| `不存在的渠道XYZ` | 0 |

公开产出：`reports/r5-enum-usage-count-summary.json`（聚合计数，无 record_id）。

#### 5.3.2 用户决策

用户决策为 `cleanup_converge_to_v1.1`：将引用 `微信` 的 1 条测试记录重映射为业务语义最接近的 v1.1 合法值 `微信私聊`，然后删除 `微信` 和 `不存在的渠道XYZ` 两个额外选项。

#### 5.3.3 清理执行（P0-1 Step 2）

清理脚本 `src/scripts/temp/r5_enum_cleanup.js`（gitignored）执行：

| 操作 | 目标 | 结果 |
|---|---|---|
| `+record-batch-update` | 1 条引用 `微信` 的测试记录 → `微信私聊` | 1 succeeded / 0 failed |
| `+field-update`（full PUT semantics） | source_channel options 14 → 12（删除 `微信`、`不存在的渠道XYZ`） | applied true / target 12 options |
| `+field-get` 重试验证 | live options 与 v1.1 spec sorted set 比较 | `live_matches_v11: true`（5 次重试内通过） |

#### 5.3.4 写后 schema 快照（重新生成）

清理后重新生成完整 V2 schema 快照（私有）：

- 路径：`backups/private/r5-v2-schema-snapshot-after-enum-cleanup.json`
- SHA256：`691e78a2244a6d44a7d02f44e603a3ed33c8e0b0cd457d4194c67aa414eeeefd`

#### 5.3.5 公开产出

| 文件 | 用途 |
|---|---|
| `reports/r5-enum-usage-count-summary.json` | 只读统计聚合计数（P0-1 Step 1） |
| `reports/r5-enum-cleanup-summary.json` | 清理执行结果与 live 验证（P0-1 Step 2） |

#### 5.3.6 已知限制

- 飞书 API 缓存延迟：`+field-update` 返回 OK 后立即 `+field-get` 可能返回旧数据。脚本采用幂等设计 + 5 次重试（间隔 3s）解决。
- `+field-update` 使用 full PUT semantics（非 patch），需先 `+field-get` 读取完整定义再发送目标状态。

---

## 6. 提交与证据

### 6.1 R5 主批次提交历史

| Commit | 任务 | 说明 |
|---|---|---|
| `7051a7f` | Task 1 | R4 控制面收口 + P1 文档债修复 |
| `c3d7e87` | Task 2 | Dry-run patch plan + 写前 schema 快照 |
| `f21b347` | Task 3 | 应用 v1.1 delta 到 V2 测试 Base |
| `9b63e8b` | Task 4 | 字段写读与拒绝验证 |
| `f373374` | Task 5 | 回滚演练 + 本报告 + R5 审计包 + 控制面收口 |
| `3df9fc5` | Task 5 backfill | SHA backfill（填入 Task 5 commit/blob SHA 引用） |

### 6.2 R5 修复批次提交历史

| Commit | 任务 | 说明 |
|---|---|---|
| `82d9886` | P0-1 + P0-2 + P0-3 | 枚举清理 + 审计包修正 + 主报告修正 + entrypoint 修正 + 安全扫描 |
| （R5 fix backfill commit） | SHA backfill | 填入 R5 修复批次的最终 HEAD 与审计包 blob/SHA256 引用 |

R5 fix main commit SHA：`82d98866686d4b0f502ad450b34177ab9a770335`。最终 HEAD 将在 backfill commit 后通过 `git rev-parse HEAD` 获取并单列（GPT 复审时由 Git 事实复核）。

### 6.3 关键公开文件 SHA256

| 文件 | SHA256 | Git blob SHA |
|---|---|---|
| `reports/r5-dry-run-patch-plan.json` | `f052ce494f084d65c8a5c228fb4dcaea9d4ec33e8c88fe3c8d00428e829b1fbf` | `1acb0cbf4cea9a0e980ec194be00c0983ab7ae9c` |
| `reports/r5-apply-summary.json` | `bf0745169d3f2aac1a3c7ff2c5737f8fdea39ae859cfb9a2552bf22147cad415` | `6ffcc483fc7b3be77324f87b0ea52abe2f6c5d12` |
| `reports/r5-field-validation-report.json` | `cecbfe1674bf2fb3969ca5abc625864f7645bcb8d53b36bc6efc4bb09f1e7747` | `6d6be4a9aba750ae8111d63191729411158a1828` |
| `reports/r5-rollback-drill-report.json` | `00d7398c4116651537fd91e2bb9e9f6ac22fbca798c0015b6df720a6756ea238` | `0201fb6170191f639d0afbe15292eec4430189dc` |
| `reports/r5-enum-usage-count-summary.json` | `66ee014e96dfb2265cacd84809d35bb00533098c9c55fd1396c0e5afc02c6f81` | `359e993d022bb9ad3c627b51fe56d2b407bcdad6`（commit `82d9886`） |
| `reports/r5-enum-cleanup-summary.json` | `40a88e53f5195dd6fd8e3ae10892f81f6546cc5627bb3cf8a41eecd52ad40b00` | `bcd0f4dfe1c00f84b5fc4d7a2868e39c2ffd957c`（commit `82d9886`） |
| `reports/v1.1-field-write-path-report.md`（本文件） | `f217e28fa1692212fabd2acef7de577716000b3ba5f15fe84caf360c69d54b4f`（R5 fix main commit `82d9886` 中的 pre-backfill SHA256）；R5 fix backfill commit 后的最终 SHA256 将由 `sha256sum` 获取并单列 | `530c359114c7cabb0dd02e0d65bb06f9d4ce2688`（R5 fix main commit `82d9886` 中的 pre-backfill blob）；R5 fix backfill commit 后的最终 blob 由 `git rev-parse HEAD:reports/v1.1-field-write-path-report.md` 获取并单列 |

### 6.4 关键私有文件 SHA256（gitignored）

| 文件 | SHA256 |
|---|---|
| `backups/private/r5-v2-schema-snapshot.json`（写前） | `eefb0bcf3fd6a69bc0d8ef2d9cefc4255e1eca4be5e4b421c5d301c4527f1d04` |
| `backups/private/r5-v2-schema-snapshot-after.json`（写后，Task 3 后） | `2a4432d798942f51c5f254286e84737223f7663d215e395165377514fec65364` |
| `backups/private/r5-v2-schema-snapshot-after-enum-cleanup.json`（R5 fix 后） | `691e78a2244a6d44a7d02f44e603a3ed33c8e0b0cd457d4194c67aa414eeeefd` |
| `backups/private/r5-apply-log.json` | `95ab830a66dc156b1816970ad8499bd7e7f69a967fb359cb1d67b56e63ef24d9` |
| `backups/private/r5-field-validation-log.json` | `a8de06fa83d2da4c6d586eee6a4f9746bb0cd166373e7e30ad7eff9d15983c8b` |
| `backups/private/r5-rollback-drill-log.json` | `18924896c861173828e0a49008b1f917011d260b15bb2583ccd905f084105d7f` |

---

## 7. Out of Scope 声明

R5 主批次（Task 1-5）：

- 未修改生产 V2 Base、V1 Base、APP 或自动化
- 未读取或写入真实客户业务记录
- 未重新运行完整迁移 Dry Run（R6）
- 未启动 MIGRATION_PILOT_001
- 未扩大 Schema v1.1（未新增 v1.1 之外的字段/枚举/视图）
- 未删除 V2 测试 Base 上 R5 之前已存在的字段/选项/视图

R5 修复批次（P0-1 + P0-2 + P0-3）：

- 未修改生产 V2 Base、V1 Base、APP 或自动化
- 未读取或写入真实客户业务记录
- 未重新运行完整迁移 Dry Run（R6）
- 未启动 MIGRATION_PILOT_001
- 未扩大 Schema v1.1
- **已删除** V2 测试 Base 中 2 个不在 v1.1 spec 内的 source_channel 选项（`微信`、`不存在的渠道XYZ`），系用户明确决策 `cleanup_converge_to_v1.1` 后执行；引用 `微信` 的 1 条测试记录已重映射为 v1.1 合法值 `微信私聊`
- 未删除 V2 测试 Base 上任何 v1.1 spec 内的字段/选项/视图

---

## 8. 控制面状态

- `R5 = R5_REVIEW_PENDING`
- `R6 = NOT_STARTED`
- `MIGRATION_PILOT_001 = NOT_APPROVED`

详细审计包见 `reports/phaseR5-v11-field-validation-gpt-audit-package.md`。
